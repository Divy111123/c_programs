#include <stdio.h>

int main() {
      int i;
      int sum = 0;
      int odd ;
      for(i = 0;i<=100;i++){
          if(i % 2 == 1){
              sum = sum + i;
          }
      }
      printf(" sum of odd numbers :%d",odd);
      return 0;
}
      
