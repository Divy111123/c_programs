#include <stdio.h>

int  Iseven(int a){
    if(a % 2 == 0){
        printf("the number is even");
    }else{
        printf(" the number is odd");
    }
}
int main() {
    Iseven(45);
    return 0;
}
